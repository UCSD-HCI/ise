<UserControl x:Class="InteractiveSpaceSDK.GUI.TrackingVizLayer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" Loaded="UserControl_Loaded">
    <Grid>
        <Canvas Name="docLayer" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
        <Canvas Name="handLayer" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
        <Canvas Name="fingerLayer" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
    </Grid>
</UserControl>
